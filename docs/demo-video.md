# Demo video script (2–3 min)

Unlisted YouTube is fine for Ready Spec Ship. Match this path to the README judge clicks.

## Setup on camera

- Browser on [https://fondof.netlify.app](https://fondof.netlify.app)
- Optional: Monad explorer tab ready for one tx after publish
- Do **not** show `.env` or private keys

## Script

| Time | Say / show |
|------|------------|
| 0:00–0:20 | **Problem:** insights from talks/blogs don’t become agent skills fitted to *your* repo. Marketplaces sell generic skills; fondof forges from source with provenance. |
| 0:20–0:35 | **Kiro:** flash `.kiro/specs/fondof/` + steering — Spec → Ship. |
| 0:35–1:10 | **Need path (no GitHub):** Need tab → `retry budgets for async TypeScript` → Extract → shards land. Mention demo repos work without OAuth. |
| 1:10–1:40 | Select Forge shards → Forge → show **skill preview** + Fit check + expandable sections (not a markdown wall). |
| 1:40–2:10 | **Publish** via relayer (or wallet). Burst “Attested on Monad.” Open skill page → score + **Proof mix** sparkline. |
| 2:10–2:35 | **I used this** → score ticks. Mention dispute stake = expensive policing; resolve = demo oracle (once, honestly). |
| 2:35–2:55 | **/pool** desk → Draw / paper cards with titles. |
| 2:55–3:10 | Close: quality loop on Monad + Kiro trail in the repo. Link README. |

## Don’t

- Don’t present offline/demo shards as live extract without saying so
- Don’t call local draft “published”
- Don’t dwell on hex / gas theater — quality story first
